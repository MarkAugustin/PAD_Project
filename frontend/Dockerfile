# Используем Node.js для запуска сервера разработки
FROM node:18

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем package.json и устанавливаем зависимости
COPY package.json ./
RUN npm install

# Копируем остальные файлы
COPY . .

# Экспонируем порт, на котором запускается сервер разработки (обычно 8080)
EXPOSE 8080

# Команда для запуска разработческого сервера
CMD ["npm", "run", "serve"]
